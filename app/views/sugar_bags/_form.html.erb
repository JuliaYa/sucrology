<%= form_for(@sugar_bag) do |f| %>
  <% if @sugar_bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sugar_bag.errors.count, "error") %> prohibited this sugar_bag from being saved:</h2>

      <ul>
      <% @sugar_bag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label :name %>:
    <%= f.text_field :name %><br />

    <%= f.label :from %>:
    <%= f.text_field :from %><br />

    <%= f.label :bags_count %>:
    <%= f.text_field :bags_count %>
    <br/>
    <%= f.submit %>
  </div>
<% end %>
